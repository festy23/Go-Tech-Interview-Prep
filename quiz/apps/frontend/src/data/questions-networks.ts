import type { Question } from "./questions";

export const questionsNetworks: Question[] = [
  // ── TCP/IP: 3-way handshake, TIME_WAIT (Q1-Q5) ──────────────────────────

  {
    id: 1,
    question:
      "Какие три пакета участвуют в установке TCP-соединения (3-way handshake) и в каком порядке они отправляются?",
    options: [
      "SYN, SYN-ACK, ACK",
      "SYN, ACK, FIN",
      "ACK, SYN, SYN-ACK",
      "FIN, FIN-ACK, ACK",
    ],
    correct: 0,
    explanation:
      "TCP-соединение устанавливается в три шага: клиент отправляет SYN (synchronize) с начальным sequence number, сервер отвечает SYN-ACK (подтверждая получение и отправляя свой sequence number), клиент завершает рукопожатие отправкой ACK. После этого соединение считается установленным и можно передавать данные.",
  },
  {
    id: 2,
    question:
      "Почему при закрытии TCP-соединения используется 4-way handshake (FIN, ACK, FIN, ACK), а не 3-way, как при установке?",
    options: [
      "Потому что TCP — полнодуплексный протокол: каждая сторона закрывает свою сторону независимо, и серверу может понадобиться время на отправку оставшихся данных",
      "Потому что 4 пакета нужны для корректной проверки контрольных сумм",
      "Потому что FIN-пакет не содержит sequence number и требует дополнительного подтверждения",
      "Потому что TCP-стек ядра всегда буферизует FIN на один RTT",
    ],
    correct: 0,
    explanation:
      "TCP — полнодуплексный протокол, каждая сторона передаёт данные независимо. Когда клиент отправляет FIN, он сообщает, что закончил передачу, но сервер может ещё иметь данные для отправки. Поэтому сервер сначала подтверждает FIN клиента (ACK), завершает отправку данных, а затем отправляет свой FIN. Объединить SYN+ACK при установке можно, потому что обе стороны готовы одновременно.",
  },
  {
    id: 3,
    question:
      "Что такое состояние TIME_WAIT в TCP и почему оно длится 2*MSL (Maximum Segment Lifetime)?",
    options: [
      "TIME_WAIT нужен, чтобы гарантировать, что последний ACK дошёл до другой стороны и что старые дубликаты пакетов не попали в новое соединение с теми же портами",
      "TIME_WAIT — это ожидание повторного SYN от клиента для восстановления соединения",
      "TIME_WAIT используется для сбора статистики о скорости соединения перед его закрытием",
      "TIME_WAIT — это буфер для повторной отправки данных, потерянных при закрытии",
    ],
    correct: 0,
    explanation:
      "Состояние TIME_WAIT возникает на стороне, инициировавшей закрытие (отправившей первый FIN). Оно длится 2*MSL (обычно 60 секунд) по двум причинам: 1) если последний ACK потерян, другая сторона повторит FIN, и нужно иметь возможность ответить; 2) чтобы старые дубликаты сегментов от предыдущего соединения не были приняты новым соединением с теми же адресами и портами.",
  },
  {
    id: 4,
    question:
      "На высоконагруженном Go-сервере наблюдается большое количество сокетов в состоянии TIME_WAIT. Какой параметр ядра Linux поможет повторно использовать такие сокеты?",
    code: "# Просмотр количества TIME_WAIT\nss -s | grep TIME-WAIT\n\n# Параметр ядра\nsysctl net.ipv4.tcp_tw_reuse=1",
    options: [
      "net.ipv4.tcp_tw_reuse позволяет повторно использовать сокеты в TIME_WAIT для новых исходящих соединений",
      "net.ipv4.tcp_fin_timeout уменьшает время TIME_WAIT до 0",
      "net.core.somaxconn увеличивает лимит TIME_WAIT сокетов",
      "net.ipv4.tcp_syncookies отключает TIME_WAIT полностью",
    ],
    correct: 0,
    explanation:
      "Параметр net.ipv4.tcp_tw_reuse=1 позволяет ядру переиспользовать сокеты в состоянии TIME_WAIT для новых исходящих соединений, если TCP timestamps включены. Параметр tcp_tw_recycle был удалён из ядра (начиная с 4.12) из-за проблем с NAT. Также полезно использовать connection pooling в Go (http.Transport с настройками MaxIdleConns) для уменьшения количества соединений.",
  },
  {
    id: 5,
    question:
      "Что такое TCP SYN flood и какой механизм ядра Linux защищает от него?",
    options: [
      "Атака заполняет очередь полуоткрытых соединений; защита — SYN cookies (net.ipv4.tcp_syncookies), позволяющие не хранить состояние до завершения handshake",
      "Атака переполняет буфер приёма данных; защита — увеличение tcp_rmem",
      "Атака отправляет FIN без установки соединения; защита — firewall на уровне iptables",
      "Атака использует фрагментацию IP-пакетов; защита — отключение фрагментации через tcp_mtu_probing",
    ],
    correct: 0,
    explanation:
      "При SYN flood атакующий отправляет множество SYN-пакетов с поддельными IP-адресами. Сервер выделяет ресурсы для каждого полуоткрытого соединения, заполняя очередь (backlog). SYN cookies кодируют информацию о соединении в sequence number SYN-ACK, что позволяет не хранить состояние до получения финального ACK от легитимного клиента.",
  },

  // ── HTTP/1.1 vs HTTP/2 (Q6-Q10) ──────────────────────────────────────────

  {
    id: 6,
    question:
      "Какая главная проблема HTTP/1.1 решается мультиплексированием в HTTP/2?",
    options: [
      "Head-of-line blocking: в HTTP/1.1 запросы в одном соединении обрабатываются последовательно, и медленный ответ блокирует все последующие",
      "Отсутствие шифрования: HTTP/1.1 не поддерживает TLS",
      "Невозможность передачи бинарных данных: HTTP/1.1 работает только с текстом",
      "Ограничение размера заголовков: HTTP/1.1 не позволяет заголовки больше 8 КБ",
    ],
    correct: 0,
    explanation:
      "В HTTP/1.1 запросы в одном TCP-соединении обрабатываются строго последовательно (head-of-line blocking). Браузеры обходят это, открывая 6-8 параллельных соединений к одному хосту. HTTP/2 решает проблему мультиплексированием: множество потоков (streams) передаются параллельно внутри одного TCP-соединения, что уменьшает количество соединений и ускоряет загрузку.",
  },
  {
    id: 7,
    question: "Какой формат передачи данных использует HTTP/2 вместо текстового?",
    options: [
      "Бинарный фреймовый протокол — каждое сообщение разбивается на фреймы (HEADERS, DATA и др.)",
      "Protocol Buffers (protobuf) — как в gRPC",
      "MessagePack — бинарный аналог JSON",
      "ASN.1 BER-кодирование — как в LDAP",
    ],
    correct: 0,
    explanation:
      "HTTP/2 использует бинарный фреймовый протокол. Каждое HTTP-сообщение разбивается на один или несколько фреймов (HEADERS, DATA, SETTINGS, PING, GOAWAY и др.), которые передаются внутри потоков (streams). Бинарный формат проще парсить машиной, менее подвержен ошибкам и позволяет мультиплексирование. gRPC построен поверх HTTP/2, но сам HTTP/2 не использует protobuf.",
  },
  {
    id: 8,
    question:
      "Что такое HPACK в контексте HTTP/2 и какую проблему он решает?",
    options: [
      "HPACK — алгоритм сжатия HTTP-заголовков, использующий статическую таблицу, динамическую таблицу и кодирование Хаффмана",
      "HPACK — протокол аутентификации, заменяющий Basic Auth в HTTP/2",
      "HPACK — механизм приоритизации потоков в HTTP/2",
      "HPACK — формат упаковки нескольких HTTP-ответов в один фрейм",
    ],
    correct: 0,
    explanation:
      "В HTTP/1.1 заголовки передаются как обычный текст и часто повторяются (Cookie, User-Agent, Accept и др.), что создаёт значительный overhead. HPACK сжимает заголовки тремя способами: статическая таблица из 61 предопределённой пары (например, :method GET), динамическая таблица для часто используемых заголовков конкретного соединения и кодирование Хаффмана для строковых значений. HPACK разработан с учётом атаки CRIME и безопасен в отличие от простого DEFLATE.",
  },
  {
    id: 9,
    question: "Как настроить Go HTTP-сервер для работы по HTTP/2?",
    code: "srv := &http.Server{\n    Addr:    \":443\",\n    Handler: mux,\n}\nlog.Fatal(srv.ListenAndServeTLS(\"cert.pem\", \"key.pem\"))",
    options: [
      "В Go HTTP/2 включён автоматически при использовании TLS (ListenAndServeTLS) — дополнительная настройка не нужна",
      "Нужно импортировать golang.org/x/net/http2 и вызвать http2.ConfigureServer()",
      "HTTP/2 работает только с fasthttp, стандартный net/http его не поддерживает",
      "Нужно установить заголовок Upgrade: h2c в каждом ответе",
    ],
    correct: 0,
    explanation:
      "Начиная с Go 1.6, пакет net/http автоматически поддерживает HTTP/2 при использовании TLS (ListenAndServeTLS). Протокол согласовывается через ALPN (Application-Layer Protocol Negotiation) во время TLS handshake. Для HTTP/2 без TLS (h2c) или для более тонкой настройки можно использовать пакет golang.org/x/net/http2, но для большинства случаев достаточно стандартного подхода. http2.ConfigureServer() из golang.org/x/net/http2 также является валидным подходом для тонкой настройки HTTP/2 параметров, но не обязателен для базового включения HTTP/2.",
  },
  {
    id: 10,
    question:
      "Что такое Server Push в HTTP/2 и почему от него в итоге отказались большинство браузеров?",
    options: [
      "Server Push позволяет серверу отправить ресурсы до запроса клиента, но на практике он часто отправлял уже закэшированные ресурсы, а 103 Early Hints оказался эффективнее",
      "Server Push — это WebSocket-подобный протокол для real-time уведомлений",
      "Server Push шифрует данные дополнительным слоем поверх TLS",
      "Server Push заменяет DNS prefetch и работает только для изображений",
    ],
    correct: 0,
    explanation:
      "Server Push позволял серверу проактивно отправлять ресурсы (CSS, JS) вместе с HTML до того, как браузер их запросит. Однако на практике сервер не знал о состоянии кэша клиента и часто отправлял уже закэшированные ресурсы, тратя трафик. Chrome удалил поддержку Server Push в версии 106. Вместо него используется 103 Early Hints, который подсказывает браузеру, что загружать, не отправляя сами данные.",
  },

  // ── TLS/HTTPS: сертификаты, mTLS (Q11-Q15) ──────────────────────────────

  {
    id: 11,
    question:
      "Какие основные этапы TLS handshake при установке HTTPS-соединения?",
    options: [
      "ClientHello → ServerHello + сертификат → обмен ключами (Key Exchange) → Finished (обе стороны подтверждают)",
      "DNS-запрос → TCP-соединение → HTTP-запрос с паролем → ответ с токеном",
      "SYN → SYN-ACK → ACK → передача сертификата в теле HTTP",
      "Обмен публичными ключами по RSA → передача данных без шифрования",
    ],
    correct: 0,
    explanation:
      "TLS handshake включает: 1) ClientHello — клиент отправляет поддерживаемые версии TLS, cipher suites и случайное число; 2) ServerHello — сервер выбирает параметры и отправляет свой сертификат; 3) Key Exchange — стороны вычисляют общий секрет (в TLS 1.3 используется Diffie-Hellman); 4) Finished — обе стороны подтверждают, что handshake завершён. После этого данные шифруются симметричным ключом.",
  },
  {
    id: 12,
    question:
      "Чем mTLS (mutual TLS) отличается от обычного TLS?",
    options: [
      "В mTLS обе стороны предъявляют сертификаты: сервер аутентифицирует клиента, а клиент — сервер",
      "mTLS использует два TLS-соединения — одно для входящего, другое для исходящего трафика",
      "mTLS шифрует данные дважды — сначала открытым ключом клиента, затем сервера",
      "mTLS работает только с самоподписанными сертификатами",
    ],
    correct: 0,
    explanation:
      "В обычном TLS только сервер предъявляет сертификат клиенту. В mTLS сервер дополнительно запрашивает сертификат у клиента (CertificateRequest) и проверяет его подпись. Это обеспечивает взаимную аутентификацию и используется в микросервисной архитектуре (service mesh, Istio), внутренних API и zero-trust сетях.",
  },
  {
    id: 13,
    question:
      "Как настроить Go HTTP-сервер для mTLS с проверкой клиентского сертификата?",
    code: "tlsConfig := &tls.Config{\n    ClientCAs:  caCertPool,\n    ClientAuth: tls.RequireAndVerifyClientCert,\n    MinVersion: tls.VersionTLS13,\n}\nsrv := &http.Server{\n    Addr:      \":443\",\n    TLSConfig: tlsConfig,\n}",
    options: [
      "Установить ClientAuth в RequireAndVerifyClientCert и добавить CA-пул в ClientCAs",
      "Установить InsecureSkipVerify в true для обхода проверки",
      "Добавить middleware, проверяющий заголовок X-Client-Cert",
      "Использовать http.ListenAndServe() вместо ListenAndServeTLS()",
    ],
    correct: 0,
    explanation:
      "Для mTLS в Go нужно: 1) создать пул CA-сертификатов (x509.CertPool), которыми подписаны клиентские сертификаты; 2) установить ClientCAs в этот пул; 3) установить ClientAuth в tls.RequireAndVerifyClientCert — сервер потребует и проверит клиентский сертификат. Другие значения ClientAuth: NoClientCert (по умолчанию), RequestClientCert (запросить, но не проверять), RequireAnyClientCert (потребовать, но не проверять). TLS 1.2 поддерживается Go для совместимости, но для новых сервисов рекомендуется TLS 1.3 как минимальная версия.",
  },
  {
    id: 14,
    question:
      "Что такое ALPN (Application-Layer Protocol Negotiation) и зачем он нужен в TLS?",
    options: [
      "ALPN — расширение TLS, позволяющее клиенту и серверу согласовать протокол прикладного уровня (h2, http/1.1) во время TLS handshake",
      "ALPN — алгоритм шифрования, заменяющий AES в TLS 1.3",
      "ALPN — механизм автоматического продления сертификатов Let's Encrypt",
      "ALPN — протокол балансировки нагрузки на уровне TCP",
    ],
    correct: 0,
    explanation:
      "ALPN (Application-Layer Protocol Negotiation) — расширение TLS, определённое в RFC 7301. Клиент отправляет в ClientHello список поддерживаемых протоколов (например, h2, http/1.1), а сервер выбирает один из них. Это позволяет согласовать HTTP/2 без дополнительного round-trip. В Go поле tls.Config.NextProtos позволяет задать поддерживаемые протоколы.",
  },
  {
    id: 15,
    question:
      "Как TLS 1.3 отличается от TLS 1.2 с точки зрения производительности handshake?",
    options: [
      "TLS 1.3 требует 1 RTT для полного handshake (вместо 2 в TLS 1.2) и поддерживает 0-RTT resumption",
      "TLS 1.3 медленнее из-за более стойких шифров",
      "TLS 1.3 не поддерживает session resumption",
      "TLS 1.3 и TLS 1.2 имеют одинаковую скорость handshake",
    ],
    correct: 0,
    explanation:
      "TLS 1.3 значительно ускоряет handshake: полный handshake занимает 1 RTT (вместо 2 в TLS 1.2), так как ClientHello сразу содержит key share. Кроме того, TLS 1.3 поддерживает 0-RTT resumption — при повторном подключении данные можно отправить с первым же пакетом. Из TLS 1.3 удалены устаревшие алгоритмы (RC4, SHA-1, RSA key exchange), оставлены только безопасные (AEAD ciphers, ECDHE).",
  },

  // ── DNS и CDN (Q16-Q20) ──────────────────────────────────────────────────

  {
    id: 16,
    question:
      "Какие серверы участвуют в рекурсивном DNS-запросе от браузера до получения IP-адреса?",
    options: [
      "Рекурсивный резолвер → корневой DNS-сервер → TLD-сервер (.com, .ru) → авторитетный DNS-сервер домена",
      "Браузер → CDN → балансировщик нагрузки → веб-сервер",
      "DNS-клиент → прокси-сервер → DNS-кэш → DHCP-сервер",
      "Рекурсивный резолвер → WHOIS-сервер → регистратор домена → хостинг",
    ],
    correct: 0,
    explanation:
      "При рекурсивном DNS-запросе: 1) клиент обращается к рекурсивному резолверу (обычно у провайдера или 8.8.8.8); 2) резолвер спрашивает корневой сервер (один из 13 кластеров), который указывает на TLD-сервер; 3) TLD-сервер (.com, .ru) указывает на авторитетный DNS-сервер домена; 4) авторитетный сервер возвращает IP-адрес. Результат кэшируется на время TTL.",
  },
  {
    id: 17,
    question:
      "Чем DNS-запись типа A отличается от CNAME?",
    options: [
      "A-запись указывает на IPv4-адрес, CNAME — на другое доменное имя (алиас)",
      "A-запись работает только для корневых доменов, CNAME — для поддоменов",
      "A-запись кэшируется, CNAME — нет",
      "A-запись используется для почты, CNAME — для веб-серверов",
    ],
    correct: 0,
    explanation:
      "A-запись (Address) напрямую отображает доменное имя на IPv4-адрес (например, example.com → 93.184.216.34). CNAME (Canonical Name) — это алиас, указывающий на другое доменное имя (например, www.example.com → example.com). CNAME нельзя использовать на apex-домене (example.com) вместе с другими записями. AAAA-запись аналогична A, но для IPv6.",
  },
  {
    id: 18,
    question:
      "Что такое TTL в DNS и как он влияет на работу Go-приложения?",
    code: "// Go по умолчанию кэширует DNS на уровне net.Resolver\n// Переопределение резолвера:\nresolver := &net.Resolver{\n    PreferGo: true,\n    Dial: customDialer,\n}",
    options: [
      "TTL определяет время кэширования DNS-ответа; Go стандартная библиотека не кэширует DNS самостоятельно — каждый Dial вызывает системный резолвер",
      "TTL — максимальное время жизни TCP-соединения в пуле http.Transport",
      "TTL — количество хопов, через которые может пройти DNS-запрос",
      "TTL определяет таймаут подключения в net.Dialer",
    ],
    correct: 0,
    explanation:
      "TTL (Time To Live) в DNS — это время в секундах, в течение которого DNS-ответ можно кэшировать. Стандартная библиотека Go (net) не имеет встроенного DNS-кэша — каждый вызов net.Dial выполняет DNS-запрос через системный резолвер (обычно /etc/resolv.conf). Кэширование происходит на уровне ОС или рекурсивного резолвера. Для высоконагруженных приложений может потребоваться сторонний DNS-кэш.",
  },
  {
    id: 19,
    question:
      "Как CDN (Content Delivery Network) ускоряет доставку контента?",
    options: [
      "CDN размещает копии контента на серверах (edge/PoP) по всему миру, пользователь получает данные от ближайшего сервера, уменьшая latency",
      "CDN сжимает весь контент в один файл для ускорения загрузки",
      "CDN кэширует DNS-записи на неограниченный срок",
      "CDN заменяет TCP на UDP для всех HTTP-запросов",
    ],
    correct: 0,
    explanation:
      "CDN — это распределённая сеть серверов (Points of Presence, PoP), расположенных в разных географических локациях. Статический контент (изображения, CSS, JS) кэшируется на edge-серверах. Пользователь получает данные от ближайшего PoP, что уменьшает latency и нагрузку на origin-сервер. CDN часто используют Anycast-маршрутизацию, где один IP-адрес маршрутизируется к ближайшему серверу.",
  },
  {
    id: 20,
    question:
      "Что такое Anycast в контексте DNS и CDN?",
    options: [
      "Anycast — метод маршрутизации, при котором один IP-адрес назначен нескольким серверам, и трафик направляется к ближайшему по сетевой топологии",
      "Anycast — протокол широковещательной рассылки DNS-ответов всем клиентам подсети",
      "Anycast — способ шифрования DNS-запросов через TLS (DoT)",
      "Anycast — механизм выбора случайного DNS-сервера из пула",
    ],
    correct: 0,
    explanation:
      "Anycast — техника маршрутизации, при которой один IP-адрес объявляется несколькими серверами в разных локациях через BGP. Маршрутизаторы направляют трафик к ближайшему серверу по метрике маршрута. Все 13 корневых DNS-кластеров используют Anycast. CDN-провайдеры (Cloudflare, Akamai) используют Anycast для минимизации latency и обеспечения отказоустойчивости.",
  },

  // ── epoll/kqueue и netpoll в Go (Q21-Q25) ────────────────────────────────

  {
    id: 21,
    question:
      "Какой механизм I/O мультиплексирования использует runtime Go на Linux?",
    options: [
      "epoll — через внутреннюю подсистему netpoller, встроенную в Go runtime",
      "select — универсальный POSIX-механизм для всех платформ",
      "io_uring — асинхронный интерфейс ядра Linux 5.1+",
      "poll — через стандартный системный вызов POSIX poll()",
    ],
    correct: 0,
    explanation:
      "Go runtime использует внутреннюю подсистему netpoller, которая абстрагирует платформенные механизмы I/O мультиплексирования: epoll на Linux, kqueue на macOS/BSD, IOCP на Windows. Netpoller интегрирован с планировщиком Go: когда горутина блокируется на I/O, она «припарковывается», а OS-поток освобождается для выполнения других горутин.",
  },
  {
    id: 22,
    question:
      "Чем epoll принципиально отличается от select и poll?",
    options: [
      "epoll работает за O(1) по количеству отслеживаемых дескрипторов — ядро запоминает список fd и уведомляет только о готовых, тогда как select/poll каждый раз сканируют весь список (O(n))",
      "epoll поддерживает только TCP, а select/poll — любые протоколы",
      "epoll доступен на всех UNIX-системах, а select/poll — только на Linux",
      "epoll блокирует процесс, а select/poll — нет",
    ],
    correct: 0,
    explanation:
      "select и poll при каждом вызове передают ядру весь список дескрипторов, ядро линейно проверяет каждый — O(n). epoll использует три системных вызова (epoll_create, epoll_ctl, epoll_wait): ядро хранит дескрипторы в red-black tree и при вызове epoll_wait возвращает только готовые дескрипторы — O(1). Это критично при тысячах соединений.",
  },
  {
    id: 23,
    question:
      "Что такое pollDesc в Go runtime и какую роль он играет в netpoller?",
    options: [
      "pollDesc — структура, связанная с каждым файловым дескриптором, хранящая состояние горутины (припаркована/готова) и seq-номер для обнаружения устаревших уведомлений",
      "pollDesc — конфигурация таймаутов для net.Conn",
      "pollDesc — буфер для хранения данных, прочитанных из сокета",
      "pollDesc — дескриптор горутины в планировщике GMP",
    ],
    correct: 0,
    explanation:
      "Каждый сетевой файловый дескриптор в Go имеет связанную структуру pollDesc. Она содержит информацию о горутине, ожидающей готовности fd к чтению или записи. Когда горутина вызывает Read/Write и данных нет, runtime «паркует» горутину через pollDesc. Поле seq инкрементируется при повторном использовании дескриптора, позволяя обнаруживать и игнорировать устаревшие уведомления от epoll/kqueue.",
  },
  {
    id: 24,
    question:
      "Что происходит, когда горутина вызывает conn.Read(), но данных в сокете ещё нет?",
    code: "func handleConn(conn net.Conn) {\n    buf := make([]byte, 1024)\n    n, err := conn.Read(buf) // что здесь происходит?\n    // ...\n}",
    options: [
      "Горутина паркуется через netpoller, OS-поток освобождается для других горутин, а когда данные появятся — epoll/kqueue разбудит горутину",
      "OS-поток блокируется на системном вызове read() до получения данных",
      "Read немедленно возвращает 0 и io.EOF",
      "Go создаёт новый OS-поток для ожидания данных",
    ],
    correct: 0,
    explanation:
      "Go использует неблокирующие сокеты. Когда conn.Read() вызывается и данных нет, runtime не блокирует OS-поток. Вместо этого: 1) fd регистрируется в netpoller (epoll/kqueue); 2) горутина переводится в состояние «припаркована» (Gwaiting); 3) OS-поток (M) забирает другую горутину из очереди P. Когда данные появятся, netpoller разбудит горутину и она продолжит выполнение.",
  },
  {
    id: 25,
    question:
      "Какой механизм I/O мультиплексирования использует Go runtime на macOS?",
    options: [
      "kqueue — BSD-аналог epoll, работающий через kevent()",
      "epoll — тот же механизм, что и на Linux",
      "Grand Central Dispatch (GCD) — фреймворк Apple",
      "poll() — стандартный POSIX-механизм",
    ],
    correct: 0,
    explanation:
      "На macOS и BSD-системах Go runtime использует kqueue через файл runtime/netpoll_kqueue.go. kqueue похож на epoll по идее (O(1), event-driven), но использует другой API: kqueue() создаёт очередь событий, kevent() добавляет/удаляет дескрипторы и ожидает события. Go абстрагирует разницу через интерфейс netpoller, поэтому разработчику не нужно думать о платформе.",
  },

  // ── Blocking vs non-blocking I/O (Q26-Q30) ──────────────────────────────

  {
    id: 26,
    question:
      "В чём разница между блокирующим и неблокирующим I/O на уровне системных вызовов?",
    options: [
      "Блокирующий read() приостанавливает поток до получения данных; неблокирующий read() немедленно возвращает EAGAIN/EWOULDBLOCK если данных нет",
      "Блокирующий I/O быстрее, потому что не тратит время на проверку состояния",
      "Неблокирующий I/O невозможен для TCP-сокетов — только для файлов",
      "Блокирующий I/O использует отдельный поток ядра для каждого вызова",
    ],
    correct: 0,
    explanation:
      "По умолчанию файловые дескрипторы в Unix находятся в блокирующем режиме: вызов read() переводит поток в состояние sleep до получения данных. При установке флага O_NONBLOCK (через fcntl) read() немедленно возвращает -1 с errno=EAGAIN если данных нет. Это позволяет одному потоку обслуживать множество соединений через event loop (epoll/kqueue).",
  },
  {
    id: 27,
    question:
      "Что такое edge-triggered и level-triggered режимы в epoll?",
    options: [
      "Level-triggered уведомляет, пока данные доступны (как poll); edge-triggered — только при изменении состояния (появлении новых данных)",
      "Edge-triggered работает только с TCP, level-triggered — с UDP",
      "Level-triggered — синхронный режим, edge-triggered — асинхронный",
      "Edge-triggered поддерживает только чтение, level-triggered — чтение и запись",
    ],
    correct: 0,
    explanation:
      "В level-triggered (LT) режиме epoll_wait вернёт дескриптор, пока в буфере есть данные для чтения — можно читать порциями. В edge-triggered (ET) режиме уведомление приходит однократно при изменении состояния. При ET необходимо читать все данные до EAGAIN, иначе они «зависнут». ET эффективнее (меньше вызовов epoll_wait), но сложнее в реализации. Go netpoller использует edge-triggered режим.",
  },
  {
    id: 28,
    question:
      "Почему Go-разработчику не нужно явно работать с неблокирующим I/O?",
    options: [
      "Go runtime автоматически переводит сокеты в неблокирующий режим и использует netpoller для парковки горутин — API остаётся синхронным",
      "Go использует блокирующий I/O для каждой горутины с отдельным OS-потоком",
      "В Go нет системных вызовов — весь I/O реализован в userspace",
      "Go использует io_uring, который не требует управления блокировкой",
    ],
    correct: 0,
    explanation:
      "Go предоставляет синхронный (blocking) API для разработчика, но внутри runtime использует неблокирующие сокеты + netpoller. Когда горутина вызывает conn.Read() и данных нет, runtime паркует горутину (а не блокирует OS-поток). Это даёт простоту синхронного кода (нет callback hell) с производительностью event-driven архитектуры. Это один из ключевых принципов дизайна Go.",
  },
  {
    id: 29,
    question:
      "Какой системный вызов Go runtime использует для перевода сокета в неблокирующий режим?",
    code: "// В упрощённом виде, внутри runtime:\n// syscall.SetNonblock(fd, true)\n// что эквивалентно:\n// fcntl(fd, F_SETFL, O_NONBLOCK)",
    options: [
      "fcntl() с флагом O_NONBLOCK (в Go — syscall.SetNonblock)",
      "ioctl() с флагом FIONBIO",
      "setsockopt() с опцией SO_NONBLOCK",
      "socket() с типом SOCK_NONBLOCK",
    ],
    correct: 0,
    explanation:
      "Go runtime использует syscall.SetNonblock(fd, true), который вызывает fcntl(fd, F_SETFL, O_NONBLOCK). Это стандартный POSIX-способ перевода файлового дескриптора в неблокирующий режим. На Linux также можно передать SOCK_NONBLOCK в socket() при создании, но для существующих fd используется fcntl. ioctl с FIONBIO — альтернативный, менее переносимый способ.",
  },
  {
    id: 30,
    question:
      "Какой шаблон используется в языках без горутин (Node.js, nginx) для обработки тысяч соединений?",
    options: [
      "Event loop: один поток с epoll/kqueue обрабатывает события от множества соединений через callback-функции",
      "Thread-per-request: один OS-поток на каждое соединение",
      "Fork-per-request: новый процесс на каждый запрос (как в CGI)",
      "Coroutine pool: фиксированное количество корутин с блокирующим I/O",
    ],
    correct: 0,
    explanation:
      "Event loop — паттерн, где один поток в цикле вызывает epoll_wait/kevent, получает готовые дескрипторы и вызывает соответствующие callback-функции. Используется в Node.js (libuv), nginx, Redis. Преимущество — минимум OS-потоков и контекстных переключений. Недостаток — callback hell и сложность кода. Go решает эту проблему, скрывая event loop в runtime и предоставляя горутины с синхронным API.",
  },

  // ── Process vs Thread vs Goroutine (Q31-Q35) ────────────────────────────

  {
    id: 31,
    question:
      "Чем процесс отличается от потока (thread) на уровне операционной системы?",
    options: [
      "Процесс имеет изолированное адресное пространство; потоки одного процесса разделяют память, файловые дескрипторы и другие ресурсы",
      "Процесс может содержать только один поток; поток может содержать несколько процессов",
      "Потоки имеют собственное адресное пространство, а процессы — общее",
      "Процесс и поток — синонимы в современных ОС",
    ],
    correct: 0,
    explanation:
      "Процесс — единица изоляции ОС: собственное адресное пространство, таблица файловых дескрипторов, PID. Потоки (threads) существуют внутри процесса и разделяют его адресное пространство, но имеют собственный стек и регистры. Переключение между потоками одного процесса дешевле, чем между процессами (не нужно переключать page tables).",
  },
  {
    id: 32,
    question:
      "Какой начальный размер стека имеет горутина в Go и почему это важно?",
    options: [
      "2 КБ с динамическим ростом до 1 ГБ — это позволяет создавать миллионы горутин с минимальным потреблением памяти",
      "64 КБ с фиксированным размером — как микропотоки",
      "1 МБ — аналогично OS-потоку, но с кооперативным переключением",
      "8 КБ без возможности роста — при переполнении возникает stack overflow",
    ],
    correct: 0,
    explanation:
      "Горутина начинается со стека в 2 КБ (с Go 1.4), который динамически растёт при необходимости (до 1 ГБ по умолчанию). OS-поток обычно получает 1-8 МБ фиксированного стека. При 2 КБ миллион горутин займёт ~2 ГБ памяти только на стеки, тогда как миллион OS-потоков потребовал бы ~1-8 ТБ. Динамический рост реализован через «stacksplit» — проверку при каждом вызове функции.",
  },
  {
    id: 33,
    question:
      "Что такое модель GMP в планировщике Go?",
    options: [
      "G (горутина) — единица работы, M (machine) — OS-поток, P (processor) — логический процессор с локальной очередью горутин",
      "G (garbage) — сборщик мусора, M (memory) — менеджер памяти, P (pool) — пул соединений",
      "G (global) — глобальный стек, M (mutex) — блокировка, P (pipeline) — конвейер обработки",
      "G (goroutine) — горутина, M (main) — главный поток, P (priority) — приоритет горутины",
    ],
    correct: 0,
    explanation:
      "В модели GMP: G (goroutine) содержит стек, указатель инструкции и состояние горутины; M (machine) — OS-поток, исполняющий код; P (processor) — логический процессор, содержащий локальную очередь G и контекст для выполнения (mcache, стек для GC). Количество P определяется GOMAXPROCS. M может выполнять горутину только при привязке к P. Если G блокируется на syscall, M отсоединяется от P, и P берёт другой M.",
  },
  {
    id: 34,
    question:
      "Что произойдёт, если горутина выполняет блокирующий системный вызов (например, чтение файла с диска)?",
    code: "func readFile() {\n    data, _ := os.ReadFile(\"/large/file\") // блокирующий syscall\n    process(data)\n}",
    options: [
      "M (OS-поток) заблокируется вместе с горутиной, P отсоединится от M и найдёт (или создаст) другой M для продолжения выполнения очереди горутин",
      "Горутина будет припаркована через netpoller, как при сетевом I/O",
      "Все горутины на данном P остановятся до завершения системного вызова",
      "Go автоматически переведёт файловый дескриптор в неблокирующий режим",
    ],
    correct: 0,
    explanation:
      "В отличие от сетевого I/O, файловый I/O на Linux является блокирующим — netpoller не может его обработать. Когда горутина выполняет блокирующий syscall, M (OS-поток) блокируется. Планировщик Go отсоединяет P от заблокированного M (handoff) и привязывает P к свободному M (или создаёт новый). Когда syscall завершится, горутина возвращается в очередь, а лишний M со временем утилизируется.",
  },
  {
    id: 35,
    question:
      "Как переключение контекста горутины отличается от переключения OS-потока?",
    options: [
      "Переключение горутины происходит в userspace (~нс) и сохраняет только несколько регистров; переключение потока требует перехода в ядро (~мкс) и сохранения полного контекста CPU",
      "Переключение горутины медленнее из-за garbage collection",
      "Переключение OS-потока быстрее благодаря аппаратной поддержке",
      "Оба переключения имеют одинаковую стоимость в современных ОС",
    ],
    correct: 0,
    explanation:
      "Переключение горутины — операция userspace: планировщик Go сохраняет ~15 регистров (SP, PC и др.), переключает стек и продолжает. Нет перехода в ядро (syscall), нет сброса TLB. Переключение OS-потока требует trap в ядро, сохранения полного контекста (все регистры, FPU, сегментные регистры), потенциального сброса TLB-кэша, что занимает 1-10 мкс. Горутины переключаются за десятки наносекунд.",
  },

  // ── Signals: SIGTERM, SIGINT (Q36-Q40) ──────────────────────────────────

  {
    id: 36,
    question:
      "Чем SIGTERM отличается от SIGKILL?",
    options: [
      "SIGTERM можно перехватить и обработать (graceful shutdown); SIGKILL — нельзя, процесс завершается немедленно ядром",
      "SIGTERM завершает только главный поток, SIGKILL — все потоки",
      "SIGKILL можно перехватить через signal.Notify, SIGTERM — нет",
      "SIGTERM отправляется при нехватке памяти (OOM), SIGKILL — при Ctrl+C",
    ],
    correct: 0,
    explanation:
      "SIGTERM (signal 15) — «вежливая» просьба завершиться: процесс может перехватить сигнал, завершить текущие запросы, закрыть соединения и корректно завершиться. SIGKILL (signal 9) — принудительное завершение: ядро немедленно убивает процесс, никакой обработчик не вызывается. Kubernetes отправляет SIGTERM, ждёт terminationGracePeriodSeconds (30 с по умолчанию) и затем SIGKILL.",
  },
  {
    id: 37,
    question:
      "Как реализовать graceful shutdown HTTP-сервера в Go?",
    code: "func main() {\n    srv := &http.Server{Addr: \":8080\"}\n\n    go func() {\n        sigCh := make(chan os.Signal, 1)\n        signal.Notify(sigCh, syscall.SIGTERM, syscall.SIGINT)\n        <-sigCh\n\n        ctx, cancel := context.WithTimeout(\n            context.Background(), 10*time.Second,\n        )\n        defer cancel()\n        srv.Shutdown(ctx)\n    }()\n\n    srv.ListenAndServe()\n}",
    options: [
      "Перехватить SIGTERM/SIGINT через signal.Notify и вызвать srv.Shutdown(ctx) с таймаутом для завершения активных соединений",
      "Вызвать srv.Close() — он автоматически дождётся завершения запросов",
      "Использовать os.Exit(0) в обработчике сигнала",
      "Отправить SIGSTOP для приостановки, затем SIGCONT для продолжения",
    ],
    correct: 0,
    explanation:
      "http.Server.Shutdown(ctx) — метод для graceful shutdown: он закрывает listener (новые соединения не принимаются), ждёт завершения активных запросов и закрывает idle-соединения. Контекст с таймаутом ограничивает время ожидания. srv.Close() — жёсткое закрытие без ожидания. os.Exit() не вызывает defer-функции. signal.NotifyContext — более идиоматичная альтернатива с Go 1.16.",
  },
  {
    id: 38,
    question:
      "Что такое signal.NotifyContext и чем он удобнее signal.Notify?",
    code: "func main() {\n    ctx, stop := signal.NotifyContext(\n        context.Background(),\n        syscall.SIGTERM, syscall.SIGINT,\n    )\n    defer stop()\n\n    srv := &http.Server{Addr: \":8080\"}\n    go func() {\n        <-ctx.Done()\n        srv.Shutdown(context.Background())\n    }()\n    srv.ListenAndServe()\n}",
    options: [
      "NotifyContext возвращает контекст, который отменяется при получении сигнала — удобно интегрировать с context-based API без ручного управления каналом",
      "NotifyContext отправляет сигнал всем дочерним процессам",
      "NotifyContext поддерживает только SIGKILL",
      "NotifyContext автоматически перезапускает процесс после сигнала",
    ],
    correct: 0,
    explanation:
      "signal.NotifyContext (Go 1.16+) возвращает контекст, который отменяется при получении указанного сигнала. Это идиоматичнее, чем ручное создание канала + signal.Notify, потому что ctx.Done() естественно интегрируется с context-aware функциями (Shutdown, database Close и др.). Функция stop() вызывается для прекращения перехвата сигналов.",
  },
  {
    id: 39,
    question:
      "Какой сигнал отправляется процессу при нажатии Ctrl+C в терминале?",
    options: [
      "SIGINT (signal 2)",
      "SIGTERM (signal 15)",
      "SIGQUIT (signal 3)",
      "SIGKILL (signal 9)",
    ],
    correct: 0,
    explanation:
      "Ctrl+C отправляет SIGINT (interrupt, signal 2) всем процессам группы переднего плана терминала. SIGINT можно перехватить в Go через signal.Notify. Ctrl+\\ отправляет SIGQUIT (signal 3), который по умолчанию генерирует core dump. SIGTERM отправляется программно (kill PID), а SIGKILL — через kill -9 PID.",
  },
  {
    id: 40,
    question:
      "Что произойдёт, если Go-программа не перехватывает SIGTERM?",
    code: "func main() {\n    // Нет signal.Notify\n    http.ListenAndServe(\":8080\", nil)\n}",
    options: [
      "Процесс завершится немедленно — Go runtime завершает программу по умолчанию при SIGTERM без вызова defer-функций",
      "Сигнал будет проигнорирован и программа продолжит работу",
      "Go автоматически выполнит graceful shutdown HTTP-сервера",
      "Произойдёт паника с трейсом всех горутин",
    ],
    correct: 0,
    explanation:
      "По умолчанию SIGTERM завершает процесс. Go runtime не обрабатывает его специальным образом — программа просто завершается. defer-функции не вызываются (нет нормального return из main). Для корректного завершения необходимо явно перехватить сигнал через signal.Notify и выполнить shutdown-логику. SIGQUIT по умолчанию вызывает panic с дампом всех горутин.",
  },

  // ── Linux CFS scheduler (Q41-Q45) ────────────────────────────────────────

  {
    id: 41,
    question:
      "Что такое CFS (Completely Fair Scheduler) в ядре Linux?",
    options: [
      "Планировщик процессов, распределяющий CPU-время пропорционально весам процессов, используя виртуальное время (vruntime) и red-black tree",
      "Файловая система для справедливого распределения дискового пространства",
      "Сетевой планировщик для балансировки нагрузки между сетевыми интерфейсами",
      "Планировщик I/O для справедливого доступа к блочным устройствам",
    ],
    correct: 0,
    explanation:
      "CFS (Completely Fair Scheduler) — планировщик процессов Linux (с ядра 2.6.23), используемый для задач класса SCHED_NORMAL. Идея: каждый процесс должен получить справедливую долю CPU. CFS отслеживает «виртуальное время» (vruntime) каждого процесса и всегда запускает процесс с наименьшим vruntime. Процессы хранятся в red-black tree, отсортированном по vruntime.",
  },
  {
    id: 42,
    question:
      "Что такое vruntime в CFS и как nice-значение на него влияет?",
    options: [
      "vruntime — виртуальное время выполнения процесса, нормализованное по весу; процессы с низким nice (высокий приоритет) накапливают vruntime медленнее",
      "vruntime — реальное время выполнения в наносекундах; nice-значение не влияет",
      "vruntime — время ожидания процесса в очереди; чем больше nice, тем меньше vruntime",
      "vruntime — виртуальная память процесса; nice определяет лимит памяти",
    ],
    correct: 0,
    explanation:
      "vruntime (virtual runtime) — ключевая метрика CFS. Это фактическое CPU-время, нормализованное по весу процесса. Процесс с nice=-20 (высокий приоритет, большой вес) за 10 мс реального времени получит меньше vruntime, чем процесс с nice=19 (низкий приоритет). CFS всегда выбирает процесс с наименьшим vruntime из rb-tree. Nice-значения варьируются от -20 до +19 (по умолчанию 0).",
  },
  {
    id: 43,
    question:
      "Какую структуру данных CFS использует для хранения процессов и почему?",
    options: [
      "Red-black tree (красно-чёрное дерево) — сбалансированное BST с O(log n) для вставки, удаления и поиска минимума (процесс с наименьшим vruntime)",
      "Hash table — для O(1) доступа по PID процесса",
      "Min-heap — для быстрого извлечения процесса с наименьшим приоритетом",
      "Linked list — для простого обхода всех процессов",
    ],
    correct: 0,
    explanation:
      "CFS хранит все запускаемые (runnable) задачи в red-black tree, отсортированном по vruntime. Leftmost node — процесс с наименьшим vruntime, который будет запущен следующим. Операции вставки и удаления — O(log n). Кэширование leftmost node позволяет получить следующий процесс за O(1). Min-heap тоже мог бы подойти, но rb-tree обеспечивает эффективную вставку и удаление в середине дерева.",
  },
  {
    id: 44,
    question:
      "Как GOMAXPROCS в Go соотносится с планировщиком CFS в Linux?",
    options: [
      "GOMAXPROCS определяет число P (логических процессоров Go), а CFS планирует M (OS-потоки Go) наравне с другими процессами — Go runtime работает поверх CFS",
      "GOMAXPROCS отключает CFS и Go сам управляет CPU-ядрами",
      "GOMAXPROCS задаёт nice-значение для всех горутин",
      "GOMAXPROCS и CFS не связаны — Go использует собственный планировщик ядра",
    ],
    correct: 0,
    explanation:
      "Go имеет двухуровневую модель планирования: Go runtime планирует горутины (G) на логические процессоры (P) — это userspace-планировщик. P привязаны к OS-потокам (M), которые CFS ядра Linux планирует на физические CPU-ядра. Таким образом, Go runtime работает поверх CFS: GOMAXPROCS определяет, сколько горутин могут выполняться параллельно, но фактическое распределение по CPU-ядрам решает CFS.",
  },
  {
    id: 45,
    question:
      "Что такое cgroup v2 cpu.max и как оно влияет на Go-приложение в контейнере?",
    code: "# Контейнер с лимитом 2 CPU:\n# cpu.max = \"200000 100000\" (200ms из каждых 100ms)\n\n# Go runtime читает лимит:\nfmt.Println(runtime.NumCPU())  // 8 (видит все ядра хоста!)\nruntime.GOMAXPROCS(0)          // 8 (по умолчанию)",
    options: [
      "cpu.max ограничивает CPU-время контейнера, но Go runtime по умолчанию видит все ядра хоста и создаёт избыточное количество P — нужно automaxprocs или ручной GOMAXPROCS",
      "cpu.max автоматически настраивает GOMAXPROCS внутри контейнера",
      "cpu.max не влияет на Go — ограничивает только CPU для системных процессов",
      "cpu.max работает только с Java и .NET — Go не поддерживает cgroups",
    ],
    correct: 0,
    explanation:
      "В контейнере с лимитом 2 CPU (cpu.max=200000/100000) Go runtime видит все ядра хоста через runtime.NumCPU(). Если хост имеет 8 ядер, GOMAXPROCS=8, что создаёт 8 P, хотя доступно только 2 CPU. Это приводит к чрезмерным переключениям контекста и throttling. Решение — пакет uber-go/automaxprocs, который читает cgroup лимиты и устанавливает корректный GOMAXPROCS.",
  },

  // ── mmap и файловые дескрипторы (Q46-Q50) ────────────────────────────────

  {
    id: 46,
    question:
      "Что такое файловый дескриптор (fd) в Linux?",
    options: [
      "Целое неотрицательное число — индекс в таблице открытых файлов процесса, через который ядро управляет доступом к файлу, сокету, pipe и другим ресурсам",
      "Уникальный путь к файлу в файловой системе",
      "Указатель на область памяти, содержащую данные файла",
      "UUID-идентификатор, присваиваемый файлу при создании",
    ],
    correct: 0,
    explanation:
      "Файловый дескриптор (fd) — целое число (int), индекс в per-process таблице открытых файлов ядра. Каждый процесс начинается с fd 0 (stdin), 1 (stdout), 2 (stderr). При open/socket/pipe ядро возвращает следующий свободный fd. В Go — os.File хранит fd внутри. Лимит дескрипторов задаётся через ulimit -n (по умолчанию 1024, для серверов обычно увеличивают до 65536+).",
  },
  {
    id: 47,
    question:
      "Что такое mmap и чем он отличается от обычного read/write?",
    options: [
      "mmap отображает файл в виртуальную память процесса — данные читаются через page fault без явных системных вызовов read/write, что эффективнее для больших файлов",
      "mmap — это блокировка файла для эксклюзивного доступа (file locking)",
      "mmap сжимает файл в памяти для экономии RAM",
      "mmap копирует весь файл в память при вызове, а read читает по частям",
    ],
    correct: 0,
    explanation:
      "mmap (memory-mapped file) отображает файл (или его часть) в виртуальное адресное пространство процесса. После mmap данные файла доступны как обычный массив байт — чтение/запись происходит через page faults ядра без вызовов read/write. Преимущества: нет копирования данных из kernel buffer в userspace (zero-copy), удобный случайный доступ, возможность разделения памяти между процессами (MAP_SHARED).",
  },
  {
    id: 48,
    question:
      "Какие флаги защиты (prot) можно установить при вызове mmap?",
    code: "// C:\nmmap(NULL, length, PROT_READ | PROT_WRITE,\n     MAP_SHARED, fd, 0);\n\n// Go:\nsyscall.Mmap(fd, 0, length,\n    syscall.PROT_READ|syscall.PROT_WRITE,\n    syscall.MAP_SHARED)",
    options: [
      "PROT_READ (чтение), PROT_WRITE (запись), PROT_EXEC (исполнение), PROT_NONE (без доступа)",
      "PROT_OPEN, PROT_CLOSE, PROT_SYNC, PROT_ASYNC",
      "PROT_USER, PROT_KERNEL, PROT_ROOT, PROT_GUEST",
      "PROT_TCP, PROT_UDP, PROT_UNIX, PROT_RAW",
    ],
    correct: 0,
    explanation:
      "mmap принимает флаги защиты: PROT_READ — разрешить чтение; PROT_WRITE — разрешить запись; PROT_EXEC — разрешить исполнение кода; PROT_NONE — запретить доступ (используется для guard pages). MAP_SHARED означает, что изменения видны другим процессам и записываются в файл. MAP_PRIVATE создаёт copy-on-write копию — изменения не влияют на файл.",
  },
  {
    id: 49,
    question:
      "Можно ли закрыть файловый дескриптор после вызова mmap?",
    code: "fd, _ := syscall.Open(\"data.bin\", syscall.O_RDONLY, 0)\ndata, _ := syscall.Mmap(fd, 0, size,\n    syscall.PROT_READ, syscall.MAP_SHARED)\nsyscall.Close(fd) // Безопасно ли это?\n// data по-прежнему доступна?",
    options: [
      "Да, закрытие fd после mmap безопасно — mapping остаётся валидным, потому что ядро удерживает ссылку на файл через vnode/inode",
      "Нет, закрытие fd немедленно разрушает mapping",
      "Да, но данные становятся read-only после закрытия fd",
      "Нет, это вызывает SIGSEGV при следующем доступе к mapping",
    ],
    correct: 0,
    explanation:
      "Согласно POSIX и man-странице mmap(2): «After the mmap() call has returned, the file descriptor can be closed immediately without invalidating the mapping». Ядро поддерживает mapping через внутренний счётчик ссылок на inode/vnode файла. Закрытие fd уменьшает счётчик, но не удаляет mapping. Mapping удаляется только при вызове munmap() или завершении процесса.",
  },
  {
    id: 50,
    question:
      "Какая проблема может возникнуть у Go-приложения при слишком низком лимите файловых дескрипторов?",
    code: "# Проверка текущего лимита:\nulimit -n\n# 1024\n\n# Увеличение:\nulimit -n 65536\n\n# Или в /etc/security/limits.conf:\n# * soft nofile 65536\n# * hard nofile 65536",
    options: [
      "При достижении лимита open/socket вернут ошибку «too many open files» — новые соединения, файлы и pipe не смогут быть открыты",
      "Процесс будет убит ядром (OOM killer) при превышении лимита",
      "Файловые дескрипторы будут переиспользоваться автоматически без ошибок",
      "Go runtime автоматически увеличивает лимит через setrlimit",
    ],
    correct: 0,
    explanation:
      "Каждое TCP-соединение, открытый файл, pipe и eventfd потребляет один файловый дескриптор. При лимите 1024 (ulimit -n) высоконагруженный Go-сервер быстро исчерпает дескрипторы. Ошибка «too many open files» (EMFILE) означает, что процесс не может открыть новые ресурсы. Для серверов рекомендуется увеличить лимит до 65536+ через ulimit или systemd (LimitNOFILE). Go runtime не увеличивает лимит автоматически.",
  },
];
